{
    "name": "koco-i18next",
    "version": "0.3.0",
    "authors": [
        "Maxime Séguin"
    ],
    "main": "src/koco-i18next.js",
    "description": "",
    "license": "MIT",
    "dependencies": {
        "jquery": "^2.1.3",
        "knockout": "^3.3.0",
        "i18next": "^1.7.7",
        "koco-mutex": "^0.2.3"
    },
    "scripts": {
        "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
    }
}
